apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "components.fullname" . }}-test-connection"
  labels:
    {{- include "components.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "components.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
