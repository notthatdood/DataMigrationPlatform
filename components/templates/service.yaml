apiVersion: v1
kind: Service
metadata:
  name: {{ include "components.fullname" . }}
  labels:
    {{- include "components.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "components.selectorLabels" . | nindent 4 }}
